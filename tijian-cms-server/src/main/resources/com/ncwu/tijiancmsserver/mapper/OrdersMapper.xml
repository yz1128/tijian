<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ncwu.tijiancmsserver.mapper.OrdersMapper">

  <resultMap id="BaseResultMap" type="com.ncwu.tijiancmsserver.model.Orders">
    <id column="orderId" jdbcType="INTEGER" property="orderid" />
    <result column="orderDate" jdbcType="DATE" property="orderdate" />
    <result column="userId" jdbcType="VARCHAR" property="userid" />
    <result column="hpId" jdbcType="INTEGER" property="hpid" />
    <result column="smId" jdbcType="INTEGER" property="smid" />
    <result column="state" jdbcType="INTEGER" property="state" />
  </resultMap>

  <sql id="Base_Column_List">
    orderId, orderDate, userId, hpId, smId, `state`
  </sql>

  <!-- 基础CRUD（由MyBatis Generator生成，保持不变） -->
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from orders
    where orderId = #{orderid,jdbcType=INTEGER}
  </select>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from orders
    where orderId = #{orderid,jdbcType=INTEGER}
  </delete>

  <insert id="insert" keyColumn="orderId" keyProperty="orderid" parameterType="com.ncwu.tijiancmsserver.model.Orders" useGeneratedKeys="true">
    insert into orders (orderDate, userId, hpId,
      smId, `state`)
    values (#{orderdate,jdbcType=DATE}, #{userid,jdbcType=VARCHAR}, #{hpid,jdbcType=INTEGER},
      #{smid,jdbcType=INTEGER}, #{state,jdbcType=INTEGER})
  </insert>

  <insert id="insertSelective" keyColumn="orderId" keyProperty="orderid" parameterType="com.ncwu.tijiancmsserver.model.Orders" useGeneratedKeys="true">
    insert into orders
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="orderdate != null">
        orderDate,
      </if>
      <if test="userid != null">
        userId,
      </if>
      <if test="hpid != null">
        hpId,
      </if>
      <if test="smid != null">
        smId,
      </if>
      <if test="state != null">
        `state`,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="orderdate != null">
        #{orderdate,jdbcType=DATE},
      </if>
      <if test="userid != null">
        #{userid,jdbcType=VARCHAR},
      </if>
      <if test="hpid != null">
        #{hpid,jdbcType=INTEGER},
      </if>
      <if test="smid != null">
        #{smid,jdbcType=INTEGER},
      </if>
      <if test="state != null">
        #{state,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>

  <update id="updateByPrimaryKeySelective" parameterType="com.ncwu.tijiancmsserver.model.Orders">
    update orders
    <set>
      <if test="orderdate != null">
        orderDate = #{orderdate,jdbcType=DATE},
      </if>
      <if test="userid != null">
        userId = #{userid,jdbcType=VARCHAR},
      </if>
      <if test="hpid != null">
        hpId = #{hpid,jdbcType=INTEGER},
      </if>
      <if test="smid != null">
        smId = #{smid,jdbcType=INTEGER},
      </if>
      <if test="state != null">
        `state` = #{state,jdbcType=INTEGER},
      </if>
    </set>
    where orderId = #{orderid,jdbcType=INTEGER}
  </update>

  <update id="updateByPrimaryKey" parameterType="com.ncwu.tijiancmsserver.model.Orders">
    update orders
    set orderDate = #{orderdate,jdbcType=DATE},
      userId = #{userid,jdbcType=VARCHAR},
      hpId = #{hpid,jdbcType=INTEGER},
      smId = #{smid,jdbcType=INTEGER},
      `state` = #{state,jdbcType=INTEGER}
    where orderId = #{orderid,jdbcType=INTEGER}
  </update>

  <!-- 定义通用动态WHERE条件片段 -->
  <sql id="selectOrdersWhere">
    <where>
      <if test="realName != null and realName != ''">
        and realName = #{realName}
      </if>
      <if test="userId != null and userId != ''">
        and userId = #{userId}
      </if>
      <if test="sex != null and sex != ''">
        and sex = #{sex}
      </if>
      <if test="smId != null and smId != ''">
        and smId = #{smId}
      </if>
      <if test="orderDate != null and orderDate != ''">
        and orderDate = #{orderDate}
      </if>
      <if test="state != null and state != ''">
        and state = #{state}
      </if>
    </where>
  </sql>

  <!-- 分页查询订单（引用动态条件片段） -->
  <select id="selectOrders" resultType="java.util.Map">
    select * from v_orders
    <include refid="selectOrdersWhere" />
    limit #{skipNum}, #{pageSize}
  </select>

  <!-- 统计符合条件的订单总数（引用动态条件片段） -->
  <select id="selectCount" resultType="java.lang.Integer">
    select count(*) from v_orders
    <include refid="selectOrdersWhere" />
  </select>

  <select id="selectById" resultType="java.util.Map">
  select * from v_orders where orderId = #{orderId}
  </select>

</mapper>